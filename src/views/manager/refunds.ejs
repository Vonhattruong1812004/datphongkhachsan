<%- include("../partials/head") %>
<%- include("../partials/header") %>
<%
  const filters = payload.filters || {};
  const rows = payload.rows || [];
  const summary = payload.summary || {};
  const statusOptions = payload.statusOptions || [];
  const statusBreakdown = payload.statusBreakdown || [];
  const page = Number(payload.currentPage || 1);
  const totalPages = Number(payload.totalPages || 1);
  const makeQuery = (overrides = {}) => new URLSearchParams({ ...filters, ...overrides }).toString();
  const pages = [];
  const firstPage = Math.max(1, page - 2);
  const lastPage = Math.min(totalPages, page + 2);
  for (let i = firstPage; i <= lastPage; i += 1) pages.push(i);
%>

<style>
  .refund-review{
    --ink:#122033;--muted:#64748b;--line:rgba(18,32,51,.11);--glass:rgba(255,255,255,.82);
    --strong:rgba(255,255,255,.94);--cyan:#06b6d4;--mint:#10b981;--sun:#f59e0b;--rose:#e11d48;
    --shadow:0 24px 70px rgba(18,32,51,.14);
    min-height:calc(100vh - 84px);padding:34px 18px 58px;color:var(--ink);
    background:
      radial-gradient(circle at 12% 12%,rgba(6,182,212,.22),transparent 28%),
      radial-gradient(circle at 88% 8%,rgba(245,158,11,.18),transparent 30%),
      radial-gradient(circle at 52% 102%,rgba(16,185,129,.18),transparent 34%),
      linear-gradient(135deg,#faffff 0%,#effcf8 46%,#fff8ed 100%);
  }
  .refund-review *{box-sizing:border-box}
  .refund-wrap{max-width:1280px;margin:0 auto}
  .refund-hero,.refund-panel,.refund-table-panel,.refund-alert{border:1px solid var(--line);background:var(--glass);box-shadow:var(--shadow);backdrop-filter:blur(20px)}
  .refund-hero{display:grid;grid-template-columns:minmax(0,1.15fr) minmax(300px,.85fr);gap:16px;border-radius:30px;padding:28px;margin-bottom:16px;overflow:hidden;position:relative}
  .refund-hero:before{content:"";position:absolute;inset:0;background:linear-gradient(115deg,rgba(6,182,212,.18),transparent 42%),linear-gradient(245deg,rgba(245,158,11,.18),transparent 44%);pointer-events:none}
  .refund-hero>*{position:relative;z-index:1}
  .refund-chip,.refund-tag,.refund-status{display:inline-flex;align-items:center;gap:8px;border-radius:999px;white-space:nowrap}
  .refund-chip{padding:7px 12px;border:1px solid rgba(6,182,212,.25);background:rgba(6,182,212,.12);color:#0e7490;font-size:12px;font-weight:1000;text-transform:uppercase}
  .refund-hero h1{margin:18px 0 10px;font-size:50px;line-height:1;letter-spacing:0;color:var(--ink)}
  .refund-hero h1 span{color:transparent;background:linear-gradient(96deg,#0891b2,#10b981 50%,#f59e0b);-webkit-background-clip:text;background-clip:text}
  .refund-lead{max-width:760px;margin:0;color:var(--muted);font-size:15px;line-height:1.7}
  .refund-actions,.refund-tags,.refund-card-actions{display:flex;flex-wrap:wrap;gap:10px}
  .refund-tags{margin-top:18px}.refund-actions{margin-top:22px}
  .refund-tag{padding:8px 11px;background:rgba(255,255,255,.72);border:1px solid var(--line);font-size:12px;font-weight:950}
  .refund-button{display:inline-flex;align-items:center;justify-content:center;min-height:40px;border-radius:999px;padding:9px 14px;border:1px solid var(--line);background:rgba(255,255,255,.80);color:var(--ink);text-decoration:none;font:inherit;font-size:13px;font-weight:950;cursor:pointer}
  .refund-button--primary{border-color:transparent;background:linear-gradient(135deg,#06b6d4,#10b981);color:white}
  .refund-button--danger{border-color:transparent;background:linear-gradient(135deg,#fb7185,#e11d48);color:white}
  .refund-kpis{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}
  .refund-kpi{border-radius:22px;padding:16px;background:rgba(255,255,255,.72);border:1px solid var(--line);min-height:98px;display:flex;flex-direction:column;justify-content:center}
  .refund-kpi strong{display:block;font-size:24px;color:var(--ink);overflow-wrap:anywhere}
  .refund-kpi span{display:block;margin-top:6px;color:var(--muted);font-size:12px;font-weight:900;text-transform:uppercase}
  .refund-layout{display:grid;grid-template-columns:330px minmax(0,1fr);gap:16px;align-items:start}
  .refund-panel,.refund-table-panel,.refund-alert{border-radius:26px;padding:18px}
  .refund-panel h2,.refund-table-panel h2{margin:0 0 12px;font-size:22px;color:var(--ink)}
  .refund-field{display:flex;flex-direction:column;gap:6px;margin-bottom:12px}
  .refund-field label{font-size:12px;font-weight:950;color:#334155}
  .refund-field input,.refund-field select,.refund-field textarea{width:100%;border:1px solid var(--line);border-radius:15px;padding:11px 12px;background:rgba(255,255,255,.88);color:var(--ink);font:inherit;outline:none}
  .refund-field textarea{min-height:82px;resize:vertical}
  .refund-breakdown{display:grid;gap:10px;margin-top:12px}
  .refund-break{border:1px solid var(--line);border-radius:18px;padding:12px;background:rgba(255,255,255,.62)}
  .refund-break strong{display:block;font-size:19px}.refund-break span{font-size:12px;color:var(--muted);font-weight:900}
  .refund-alert{margin-bottom:14px}.refund-alert--ok{background:rgba(16,185,129,.12);color:#065f46}.refund-alert--err{background:rgba(225,29,72,.12);color:#9f1239}
  .refund-table-wrap{overflow:auto;border:1px solid var(--line);border-radius:20px;background:rgba(255,255,255,.62)}
  .refund-table{width:100%;border-collapse:collapse;min-width:1120px;font-size:13px}
  .refund-table th,.refund-table td{padding:13px;border-bottom:1px solid rgba(18,32,51,.08);text-align:left;vertical-align:top}
  .refund-table th{background:rgba(255,255,255,.84);font-size:11px;text-transform:uppercase;color:#64748b;letter-spacing:0;font-weight:1000}
  .refund-table tr:last-child td{border-bottom:0}
  .refund-main{font-weight:1000;color:var(--ink)}.refund-sub{margin-top:4px;color:var(--muted);font-size:12px;line-height:1.45}
  .refund-status{padding:5px 9px;font-size:11px;font-weight:1000;border:1px solid rgba(100,116,139,.18);background:rgba(100,116,139,.10);color:#475569}
  .refund-status--sun{background:rgba(245,158,11,.15);color:#92400e;border-color:rgba(245,158,11,.28)}
  .refund-status--cyan{background:rgba(6,182,212,.12);color:#0e7490;border-color:rgba(6,182,212,.24)}
  .refund-status--green{background:rgba(16,185,129,.12);color:#047857;border-color:rgba(16,185,129,.24)}
  .refund-status--rose{background:rgba(225,29,72,.10);color:#be123c;border-color:rgba(225,29,72,.22)}
  .refund-empty{border:1px dashed rgba(18,32,51,.16);border-radius:24px;padding:24px;color:var(--muted);background:rgba(255,255,255,.60)}
  .refund-pages{display:flex;flex-wrap:wrap;gap:8px;justify-content:flex-end;margin-top:14px}
  .refund-page{min-width:38px;min-height:38px;border-radius:14px;border:1px solid var(--line);display:inline-flex;align-items:center;justify-content:center;text-decoration:none;color:var(--ink);background:rgba(255,255,255,.72);font-size:13px;font-weight:950}
  .refund-page.is-active{background:linear-gradient(135deg,#06b6d4,#10b981);color:white;border-color:transparent}
  @media(max-width:980px){.refund-hero,.refund-layout{grid-template-columns:1fr}.refund-hero h1{font-size:40px}}
  @media(max-width:640px){.refund-review{padding:22px 12px 42px}.refund-kpis{grid-template-columns:1fr}.refund-table-panel{padding:12px}}

  /* Quan ly UC4 visual pass only: separate refund review work zones from fields and data. */
  .refund-review{
    --ink:#eaf8ff;
    --ink-dark:#102033;
    --muted:#aac0d8;
    --muted-dark:#5f7286;
    --line:rgba(148,210,255,.24);
    --glass:rgba(8,18,39,.72);
    --strong:rgba(10,25,55,.90);
    --field:#ffffff;
    --field-line:#b7cfe1;
    --cyan:#22d3ee;
    --mint:#34d399;
    --sun:#facc15;
    --rose:#fb7185;
    --violet:#a78bfa;
    --shadow:0 28px 90px rgba(0,0,0,.38);
    position:relative;
    isolation:isolate;
    overflow:hidden;
    color:var(--ink);
    background:
      radial-gradient(circle at 12% 10%,rgba(34,211,238,.20),transparent 26%),
      radial-gradient(circle at 84% 6%,rgba(167,139,250,.18),transparent 28%),
      radial-gradient(circle at 78% 88%,rgba(251,113,133,.16),transparent 30%),
      linear-gradient(145deg,#061321 0%,#0a1630 45%,#180d35 100%);
  }
  .refund-review:before,
  .refund-review:after{
    content:"";
    position:absolute;
    inset:0;
    pointer-events:none;
    z-index:-1;
  }
  .refund-review:before{
    opacity:.82;
    background-image:
      radial-gradient(circle,#f8fbff 0 1.2px,transparent 1.7px),
      radial-gradient(circle,#8ee7ff 0 1px,transparent 1.5px),
      radial-gradient(circle,#ffffff 0 1px,transparent 1.4px);
    background-size:120px 120px,180px 180px,260px 260px;
    background-position:12px 20px,70px 42px,28px 96px;
    animation:refund-star-drift 44s linear infinite;
  }
  .refund-review:after{
    background:
      linear-gradient(115deg,transparent 0 45%,rgba(34,211,238,.18) 46%,transparent 47% 100%),
      linear-gradient(25deg,transparent 0 62%,rgba(250,204,21,.12) 63%,transparent 64% 100%);
    opacity:.5;
  }
  .refund-wrap{position:relative;z-index:1;max-width:1320px}
  .refund-hero,
  .refund-panel,
  .refund-table-panel,
  .refund-alert{
    border:1px solid var(--line);
    background:linear-gradient(180deg,rgba(12,28,61,.88),rgba(6,16,36,.78));
    box-shadow:var(--shadow);
    backdrop-filter:blur(18px);
  }
  .refund-hero{
    grid-template-columns:minmax(0,1.08fr) minmax(360px,.92fr);
    gap:22px;
    border-radius:34px;
    padding:32px;
    margin-bottom:20px;
    min-height:330px;
  }
  .refund-hero:before{
    background:
      radial-gradient(circle at 84% 42%,rgba(34,211,238,.18),transparent 24%),
      linear-gradient(115deg,rgba(34,211,238,.14),transparent 42%),
      linear-gradient(245deg,rgba(250,204,21,.11),transparent 44%);
  }
  .refund-hero:after{
    content:"";
    position:absolute;
    right:-54px;
    top:-78px;
    width:330px;
    height:330px;
    border-radius:50%;
    border:1px dashed rgba(148,210,255,.24);
    box-shadow:inset 0 0 70px rgba(34,211,238,.12),0 0 70px rgba(34,211,238,.12);
    animation:refund-orbit-spin 28s linear infinite;
  }
  .refund-chip{
    border-color:rgba(34,211,238,.36);
    background:rgba(34,211,238,.14);
    color:#cffafe;
    box-shadow:0 0 0 6px rgba(34,211,238,.07);
  }
  .refund-hero h1{
    max-width:760px;
    color:#f8fbff;
    font-size:clamp(42px,5.5vw,70px);
    line-height:.92;
    margin:20px 0 14px;
  }
  .refund-hero h1 span{
    background:linear-gradient(95deg,#22d3ee 0%,#34d399 38%,#facc15 70%,#fb7185 100%);
    -webkit-background-clip:text;
    background-clip:text;
  }
  .refund-lead{
    color:#c4d7ea;
    font-size:16px;
    line-height:1.75;
  }
  .refund-tag{
    color:#eaf8ff;
    background:rgba(255,255,255,.08);
    border-color:rgba(148,210,255,.22);
  }
  .refund-button{
    min-height:44px;
    border-color:rgba(148,210,255,.24);
    background:rgba(255,255,255,.08);
    color:#f8fbff;
    box-shadow:0 14px 34px rgba(0,0,0,.14);
  }
  .refund-button:hover{
    transform:translateY(-1px);
    border-color:rgba(34,211,238,.44);
    box-shadow:0 20px 48px rgba(34,211,238,.13);
  }
  .refund-button--primary{
    background:linear-gradient(135deg,#06b6d4,#10b981);
    color:#04111f;
    font-weight:1000;
    box-shadow:0 20px 48px rgba(16,185,129,.24);
  }
  .refund-button--danger{
    background:linear-gradient(135deg,#fb7185,#e11d48);
    color:#fff;
    box-shadow:0 18px 44px rgba(225,29,72,.22);
  }
  .refund-kpis{
    gap:14px;
    align-content:stretch;
  }
  .refund-kpi{
    position:relative;
    overflow:hidden;
    border:1px solid rgba(255,255,255,.18);
    background:linear-gradient(135deg,#22d3ee,#2563eb);
    min-height:124px;
    padding:18px;
    box-shadow:0 18px 44px rgba(0,0,0,.18);
  }
  .refund-kpi:nth-child(2){background:linear-gradient(135deg,#34d399,#14b8a6)}
  .refund-kpi:nth-child(3){background:linear-gradient(135deg,#fb7185,#a855f7)}
  .refund-kpi:nth-child(4){background:linear-gradient(135deg,#facc15,#fb923c)}
  .refund-kpi:before{
    content:"";
    position:absolute;
    left:14px;
    top:14px;
    width:54px;
    height:54px;
    border-radius:50%;
    background:rgba(255,255,255,.20);
    box-shadow:inset 0 0 0 1px rgba(255,255,255,.28);
  }
  .refund-kpi strong,
  .refund-kpi span{
    position:relative;
    z-index:1;
    color:#fff;
    text-shadow:0 1px 12px rgba(0,0,0,.18);
  }
  .refund-kpi strong{font-size:26px}
  .refund-kpi span{font-size:12px;color:rgba(255,255,255,.86)}
  .refund-layout{
    grid-template-columns:360px minmax(0,1fr);
    gap:20px;
  }
  .refund-panel,
  .refund-table-panel{
    border-radius:28px;
    padding:20px;
  }
  .refund-panel h2,
  .refund-table-panel h2{
    color:#f8fbff;
    font-size:24px;
    margin-bottom:16px;
  }
  .refund-field label{
    color:#d8e7f7;
    font-size:13px;
    letter-spacing:.02em;
  }
  .refund-field input,
  .refund-field select,
  .refund-field textarea{
    min-height:48px;
    border:2px solid var(--field-line);
    border-radius:16px;
    background:var(--field);
    color:var(--ink-dark);
    box-shadow:0 9px 0 rgba(148,210,255,.10),0 16px 34px rgba(0,0,0,.10);
  }
  .refund-field input:focus,
  .refund-field select:focus,
  .refund-field textarea:focus{
    border-color:#22d3ee;
    box-shadow:0 0 0 5px rgba(34,211,238,.18),0 16px 34px rgba(0,0,0,.12);
  }
  .refund-break,
  .refund-empty{
    border:1px solid rgba(183,207,225,.75);
    background:linear-gradient(180deg,#ffffff,#eef8ff);
    color:var(--ink-dark);
    box-shadow:0 14px 34px rgba(0,0,0,.10);
  }
  .refund-break strong{color:#0b253f}
  .refund-break span,
  .refund-empty{color:var(--muted-dark)}
  .refund-table-wrap{
    border:1px solid rgba(183,207,225,.78);
    background:#ffffff;
    box-shadow:0 18px 48px rgba(0,0,0,.16);
  }
  .refund-table{
    color:var(--ink-dark);
    font-size:14px;
  }
  .refund-table th{
    position:sticky;
    top:0;
    z-index:1;
    background:linear-gradient(180deg,#e8f7ff,#dff2fb);
    color:#173650;
    border-bottom:1px solid rgba(183,207,225,.90);
    font-size:12px;
  }
  .refund-table td{
    background:#ffffff;
    border-bottom:1px solid rgba(183,207,225,.68);
  }
  .refund-table tr:nth-child(even) td{background:#f5fbff}
  .refund-main{color:#0b253f;font-size:14px}
  .refund-sub{color:#5d7185;font-size:13px}
  .refund-status{
    border-width:1px;
    background:#eef6ff;
    color:#334155;
  }
  .refund-status--sun{background:#fff7d6;color:#854d0e;border-color:#fde68a}
  .refund-status--cyan{background:#cffafe;color:#0e7490;border-color:#67e8f9}
  .refund-status--green{background:#d1fae5;color:#047857;border-color:#86efac}
  .refund-status--rose{background:#ffe4e6;color:#be123c;border-color:#fda4af}
  .refund-alert--ok{
    background:linear-gradient(135deg,rgba(16,185,129,.24),rgba(6,182,212,.18));
    color:#d1fae5;
    border-color:rgba(52,211,153,.35);
  }
  .refund-alert--err{
    background:linear-gradient(135deg,rgba(225,29,72,.22),rgba(251,113,133,.16));
    color:#ffe4e6;
    border-color:rgba(251,113,133,.36);
  }
  .refund-pages{gap:10px}
  .refund-page{
    border-color:rgba(148,210,255,.26);
    background:rgba(255,255,255,.10);
    color:#eaf8ff;
  }
  .refund-page.is-active{
    background:linear-gradient(135deg,#22d3ee,#10b981);
    color:#04111f;
  }
  @keyframes refund-star-drift{
    from{background-position:12px 20px,70px 42px,28px 96px}
    to{background-position:132px 140px,250px 222px,288px 356px}
  }
  @keyframes refund-orbit-spin{
    to{transform:rotate(360deg)}
  }
  @media(max-width:980px){
    .refund-hero,
    .refund-layout{grid-template-columns:1fr}
    .refund-hero{padding:24px;border-radius:28px}
    .refund-hero h1{font-size:42px}
    .refund-hero:after{display:none}
  }
  @media(max-width:640px){
    .refund-review{padding:22px 12px 42px}
    .refund-kpis{grid-template-columns:1fr}
    .refund-table-panel,
    .refund-panel{padding:16px;border-radius:24px}
    .refund-actions{gap:8px}
    .refund-button{width:100%}
  }

  /* Manager refunds cleanup: keep only the fixed global galaxy background. */
  .refund-review{
    overflow:visible !important;
    background:
      linear-gradient(135deg,rgba(3,17,31,.86) 0%,rgba(7,22,52,.80) 48%,rgba(22,13,51,.84) 100%) !important;
  }
  .refund-review:before,
  .refund-review:after,
  .refund-hero:after{
    display:none !important;
    content:none !important;
    animation:none !important;
  }
  .refund-hero,
  .refund-panel,
  .refund-table-panel,
  .refund-alert{
    -webkit-backdrop-filter:none !important;
    backdrop-filter:none !important;
    will-change:auto !important;
  }
  .refund-hero{
    min-height:auto;
    background:
      radial-gradient(circle at 88% 16%,rgba(34,211,238,.16),transparent 30%),
      linear-gradient(145deg,rgba(8,13,31,.95),rgba(15,23,42,.84)) !important;
  }
  .refund-panel,
  .refund-table-panel{
    background:linear-gradient(180deg,rgba(8,13,31,.94),rgba(9,20,40,.90)) !important;
  }
  .refund-field input,
  .refund-field select,
  .refund-field textarea{
    background:#ffffff !important;
    border-color:#b7cfe1 !important;
    color:#102033 !important;
  }
  .refund-table-wrap,
  .refund-break,
  .refund-empty{
    background:#ffffff !important;
  }
  .refund-table td{
    background:#ffffff !important;
  }
  .refund-table tr:nth-child(even) td{
    background:#f5fbff !important;
  }

  /* Manager refunds UC: compact white/blue operations layout. */
  .refund-review {
    max-width: 1380px;
    min-height: calc(100vh - 69px);
    margin: 0 auto;
    padding: 12px 18px 42px;
    color: #102033;
    background:
      linear-gradient(180deg, #eaf4ff 0%, #f8fbff 220px, #ffffff 100%) !important;
  }
  .refund-wrap {
    max-width: none;
  }
  .refund-hero {
    display: grid;
    grid-template-columns: 1fr;
    gap: 10px;
    min-height: 0;
    margin-bottom: 10px;
    padding: 0;
    border: 0;
    background: transparent !important;
    box-shadow: none;
  }
  .refund-hero::before {
    display: none;
    content: none;
  }
  .refund-chip,
  .refund-lead,
  .refund-hero > div:first-child .refund-tags,
  .refund-hero > div:first-child .refund-actions {
    display: none;
  }
  .refund-hero > div:first-child {
    padding: 13px 16px;
    border: 1px solid #bfdcff;
    border-radius: 8px;
    color: #ffffff;
    background: linear-gradient(135deg, #006ce4 0%, #0b78ea 52%, #003b95 100%);
    box-shadow: 0 14px 34px rgba(0, 59, 149, .16);
  }
  .refund-hero h1 {
    width: max-content;
    max-width: 100%;
    margin: 0;
    padding-left: 11px;
    border-left: 5px solid #ffffff;
    color: #ffffff;
    font-size: 25px;
    line-height: 1.14;
    text-shadow: none;
  }
  .refund-hero h1 span {
    color: #ffffff;
    background: none;
    -webkit-background-clip: initial;
    background-clip: initial;
  }
  .refund-kpis {
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 8px;
  }
  .refund-kpi,
  .refund-kpi:nth-child(n) {
    min-height: 68px;
    padding: 11px 13px;
    border: 1px solid #cfe5ff;
    border-radius: 8px;
    color: #003b95;
    background: #ffffff;
    box-shadow: 0 8px 18px rgba(0, 59, 149, .06);
  }
  .refund-kpi::before {
    display: none;
  }
  .refund-kpi strong,
  .refund-kpi span {
    color: inherit;
    text-shadow: none;
  }
  .refund-kpi strong {
    font-size: 22px;
    line-height: 1.05;
  }
  .refund-kpi span {
    margin-top: 6px;
    color: #64748b;
    font-size: 11px;
    letter-spacing: 0;
    text-transform: none;
  }
  .refund-layout {
    grid-template-columns: 310px minmax(0, 1fr);
    gap: 10px;
  }
  .refund-panel,
  .refund-table-panel,
  .refund-alert {
    border: 1px solid #cfe5ff;
    border-radius: 8px;
    color: #102033;
    background: #ffffff !important;
    box-shadow: 0 10px 24px rgba(0, 59, 149, .08);
  }
  .refund-panel,
  .refund-table-panel {
    padding: 12px;
  }
  .refund-panel h2,
  .refund-table-panel h2 {
    margin: 0 0 10px;
    color: #102033;
    font-size: 18px;
    line-height: 1.2;
  }
  .refund-field {
    gap: 5px;
    margin-bottom: 9px;
  }
  .refund-field label {
    color: #102033;
    font-size: 11px;
    letter-spacing: 0;
    text-transform: none;
  }
  .refund-field input,
  .refund-field select,
  .refund-field textarea {
    min-height: 38px;
    border: 1px solid #cfe5ff !important;
    border-radius: 8px;
    padding: 8px 10px;
    background: #ffffff !important;
    color: #102033 !important;
    box-shadow: none;
  }
  .refund-field textarea {
    min-height: 76px;
  }
  .refund-field input:focus,
  .refund-field select:focus,
  .refund-field textarea:focus {
    border-color: #006ce4 !important;
    box-shadow: 0 0 0 3px rgba(0, 108, 228, .12);
  }
  .refund-actions,
  .refund-card-actions {
    gap: 7px;
  }
  .refund-button {
    min-height: 36px;
    border-radius: 8px;
    border-color: #bfdbfe;
    padding: 8px 12px;
    color: #075eb8;
    background: #ffffff;
    box-shadow: none;
  }
  .refund-button:hover {
    transform: none;
    box-shadow: 0 8px 18px rgba(0, 108, 228, .10);
  }
  .refund-button--primary {
    color: #ffffff;
    border-color: #006ce4;
    background: #006ce4;
    box-shadow: 0 10px 22px rgba(0, 108, 228, .18);
  }
  .refund-button--danger {
    color: #ffffff;
    border-color: #dc2626;
    background: #dc2626;
    box-shadow: 0 10px 22px rgba(220, 38, 38, .16);
  }
  .refund-breakdown {
    gap: 7px;
    margin-top: 0;
  }
  .refund-break,
  .refund-empty {
    border: 1px solid #cfe5ff;
    border-radius: 8px;
    padding: 10px;
    background: #f7fbff !important;
    color: #64748b;
    box-shadow: none;
  }
  .refund-break strong {
    color: #003b95;
    font-size: 17px;
  }
  .refund-break span {
    color: #64748b;
    font-size: 12px;
    text-transform: none;
  }
  .refund-table-wrap {
    overflow: visible;
    border: 0;
    border-radius: 0;
    background: transparent !important;
    box-shadow: none;
  }
  .refund-table {
    display: block;
    width: 100%;
    min-width: 0;
    border-collapse: separate;
    border-spacing: 0;
  }
  .refund-table thead {
    display: none;
  }
  .refund-table tbody {
    display: grid;
    gap: 10px;
  }
  .refund-table tr {
    display: grid;
    grid-template-columns: .95fr 1.15fr 1fr .9fr .8fr;
    gap: 8px;
    padding: 10px;
    border: 1px solid #cfe5ff;
    border-radius: 8px;
    background: #ffffff;
    box-shadow: 0 8px 18px rgba(0, 59, 149, .05);
  }
  .refund-table td,
  .refund-table tr:nth-child(even) td {
    display: block;
    min-width: 0;
    padding: 10px;
    border: 1px solid #e0efff;
    border-radius: 8px;
    background: #ffffff !important;
    color: #102033;
    overflow-wrap: anywhere;
  }
  .refund-table td::before {
    content: attr(data-label);
    display: block;
    margin-bottom: 6px;
    color: #006ce4;
    font-size: 10px;
    font-weight: 950;
    letter-spacing: 0;
    text-transform: none;
  }
  .refund-table td:nth-child(6) {
    grid-column: 1 / span 3;
    background: #f7fbff !important;
  }
  .refund-table td:nth-child(7) {
    grid-column: 4 / span 2;
    background: #f7fbff !important;
  }
  .refund-main {
    color: #003b95;
    font-size: 14px;
  }
  .refund-sub {
    color: #64748b;
    font-size: 12px;
  }
  .refund-status {
    border-radius: 999px;
    padding: 5px 9px;
    background: #eff6ff;
    color: #075eb8;
  }
  .refund-card-actions {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
  .refund-card-actions .refund-button {
    width: 100%;
  }
  .refund-pages {
    justify-content: flex-end;
    margin-top: 10px;
  }
  .refund-page {
    min-width: 34px;
    min-height: 34px;
    border-radius: 8px;
    border-color: #bfdbfe;
    background: #ffffff;
    color: #075eb8;
  }
  .refund-page.is-active {
    color: #ffffff;
    background: #006ce4;
    border-color: #006ce4;
  }
  @media (max-width: 1180px) {
    .refund-layout {
      grid-template-columns: 1fr;
    }
    .refund-table tr {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
    .refund-table td:nth-child(6),
    .refund-table td:nth-child(7) {
      grid-column: auto;
    }
  }
  @media (max-width: 760px) {
    .refund-review {
      padding-left: 12px;
      padding-right: 12px;
    }
    .refund-kpis,
    .refund-table tr,
    .refund-card-actions {
      grid-template-columns: 1fr;
    }
  }
</style>

<main class="refund-review">
  <div class="refund-wrap">
    <section class="refund-hero">
      <div>
        <div class="refund-chip">Manager refund approval</div>
        <h1>Duyệt <span>hoàn tiền</span></h1>
        <p class="refund-lead">Quản lý kiểm tra yêu cầu hoàn cọc do Khách hàng/Lễ tân tạo sau khi hủy đặt phòng. Khi duyệt, yêu cầu chuyển sang Kế toán để chi hoàn và ghi phiếu chi.</p>
        <div class="refund-tags">
          <span class="refund-tag">Chờ duyệt: <%= summary.managerPendingCount || 0 %></span>
          <span class="refund-tag">Đã chuyển kế toán: <%= summary.accountingReadyCount || 0 %></span>
          <span class="refund-tag">Kỳ lọc: <%= payload.rangeLabel %></span>
        </div>
        <div class="refund-actions">
          <a class="refund-button refund-button--primary" href="/manager/refunds?trang_thai=ChoQuanLyDuyet">Yêu cầu chờ duyệt</a>
          <a class="refund-button" href="/dashboard/quanly">Dashboard</a>
        </div>
      </div>
      <div class="refund-kpis">
        <div class="refund-kpi"><strong><%= summary.managerPendingAmountFormatted || "0 đ" %></strong><span>Đang chờ quản lý</span></div>
        <div class="refund-kpi"><strong><%= summary.accountingReadyAmountFormatted || "0 đ" %></strong><span>Đã chuyển kế toán</span></div>
        <div class="refund-kpi"><strong><%= summary.rejectedAmountFormatted || "0 đ" %></strong><span>Đã từ chối</span></div>
        <div class="refund-kpi"><strong><%= payload.totalRecords || 0 %></strong><span>Yêu cầu</span></div>
      </div>
    </section>

    <% if (notice?.success) { %><div class="refund-alert refund-alert--ok"><%= notice.success %></div><% } %>
    <% if (notice?.error) { %><div class="refund-alert refund-alert--err"><%= notice.error %></div><% } %>

    <section class="refund-layout">
      <aside>
        <section class="refund-panel">
          <h2>Bộ lọc</h2>
          <form method="get" action="/manager/refunds">
            <div class="refund-field">
              <label for="tu_ngay">Từ ngày</label>
              <input id="tu_ngay" name="tu_ngay" type="date" value="<%= filters.tu_ngay %>" />
            </div>
            <div class="refund-field">
              <label for="den_ngay">Đến ngày</label>
              <input id="den_ngay" name="den_ngay" type="date" value="<%= filters.den_ngay %>" />
            </div>
            <div class="refund-field">
              <label for="trang_thai">Trạng thái</label>
              <select id="trang_thai" name="trang_thai">
                <% statusOptions.forEach((item) => { %>
                  <option value="<%= item.value %>" <%= item.value === filters.trang_thai ? "selected" : "" %>><%= item.label %></option>
                <% }) %>
              </select>
            </div>
            <div class="refund-field">
              <label for="search">Tìm kiếm</label>
              <input id="search" name="search" value="<%= filters.search || "" %>" placeholder="Mã refund, GD, khách, STK..." />
            </div>
            <div class="refund-actions">
              <button class="refund-button refund-button--primary" type="submit">Lọc</button>
              <a class="refund-button" href="/manager/refunds">Đặt lại</a>
            </div>
          </form>
        </section>

        <% if (statusBreakdown.length) { %>
          <section class="refund-panel" style="margin-top:16px">
            <h2>Theo trạng thái</h2>
            <div class="refund-breakdown">
              <% statusBreakdown.forEach((item) => { %>
                <div class="refund-break">
                  <strong><%= item.totalFormatted %></strong>
                  <span><%= item.meta?.label || item.status %> · <%= item.count %> yêu cầu</span>
                </div>
              <% }) %>
            </div>
          </section>
        <% } %>
      </aside>

      <section class="refund-table-panel">
        <h2>Yêu cầu hoàn tiền cần duyệt</h2>
        <% if (!rows.length) { %>
          <div class="refund-empty">Chưa có yêu cầu hoàn tiền phù hợp bộ lọc.</div>
        <% } else { %>
          <div class="refund-table-wrap">
            <table class="refund-table">
              <thead>
                <tr>
                  <th>Yêu cầu</th>
                  <th>Khách / giao dịch</th>
                  <th>Thông tin hoàn</th>
                  <th>Policy</th>
                  <th>Trạng thái</th>
                  <th>Ghi chú</th>
                  <th>Thao tác</th>
                </tr>
              </thead>
              <tbody>
                <% rows.forEach((row) => { %>
                  <tr>
                    <td data-label="Yêu cầu">
                      <div class="refund-main"><%= row.refundCode %></div>
                      <div class="refund-sub">Tạo lúc <%= row.createdAtLabel %></div>
                      <div class="refund-sub"><%= row.roomCount %> phòng · <%= row.scope === "partial" ? "Hủy một phần" : "Hủy toàn bộ" %></div>
                    </td>
                    <td data-label="Khách / giao dịch">
                      <div class="refund-main">GD-<%= row.maGiaoDich %></div>
                      <div class="refund-sub"><%= row.bookingCode || "Không có mã đặt chỗ" %> · <%= row.transactionStatus || "" %></div>
                      <div class="refund-sub"><%= row.customerName || "Khách" %> · <%= row.customerPhone || row.customerEmail || "Chưa có liên hệ" %></div>
                      <div class="refund-sub"><%= row.hotelLabel %></div>
                    </td>
                    <td data-label="Thông tin hoàn">
                      <div class="refund-main"><%= row.amountRequestedFormatted %></div>
                      <div class="refund-sub"><%= row.bankName %> · <%= row.bankAccountNo %></div>
                      <div class="refund-sub"><%= row.bankAccountName %></div>
                      <div class="refund-sub">Cọc đã thu: <%= row.depositPaidFormatted %></div>
                    </td>
                    <td data-label="Policy">
                      <div class="refund-main"><%= row.cancellationPolicyLabel || row.refundRateLabel %></div>
                      <div class="refund-sub">Còn <%= row.hoursBeforeCheckinLabel %> tới check-in</div>
                      <div class="refund-sub">Cọc xét hoàn: <%= row.refundableBaseFormatted %></div>
                      <div class="refund-sub">Giữ lại: <%= row.retainedDepositFormatted %></div>
                    </td>
                    <td data-label="Trạng thái">
                      <span class="refund-status refund-status--<%= row.statusMeta?.tone || "slate" %>"><%= row.statusMeta?.label || row.status %></span>
                      <% if (row.managerReviewedAtLabel) { %><div class="refund-sub">QL: <%= row.managerReviewedAtLabel %></div><% } %>
                      <% if (row.expenseId) { %><div class="refund-sub">Phiếu chi CP-<%= row.expenseId %></div><% } %>
                    </td>
                    <td data-label="Ghi chú">
                      <div class="refund-sub"><strong>Lý do hủy:</strong> <%= row.reason || "Không có" %></div>
                      <% if (row.cancellationPolicyNote) { %><div class="refund-sub"><strong>Policy:</strong> <%= row.cancellationPolicyNote %></div><% } %>
                      <% if (row.note) { %><div class="refund-sub"><strong>Lễ tân:</strong> <%= row.note %></div><% } %>
                      <% if (row.managerNote) { %><div class="refund-sub"><strong>Quản lý:</strong> <%= row.managerNote %></div><% } %>
                      <% if (row.accountingNote) { %><div class="refund-sub"><strong>Kế toán:</strong> <%= row.accountingNote %></div><% } %>
                    </td>
                    <td data-label="Thao tác">
                      <% if (row.canReview) { %>
                        <form method="post" action="/manager/refunds/review" style="display:grid;gap:8px;min-width:230px">
                          <input type="hidden" name="refund_id" value="<%= row.id %>" />
                          <div class="refund-field" style="margin:0">
                            <textarea name="manager_note" placeholder="Ghi chú duyệt/từ chối..."></textarea>
	                          </div>
	                          <div class="refund-card-actions">
	                            <button class="refund-button refund-button--primary" type="submit" name="action" value="approve" <%= Number(row.amountRequested || 0) <= 0 ? "disabled title='Yêu cầu 0đ chỉ cần từ chối/ghi nhận không hoàn.'" : "" %> onclick="return confirm('Duyệt yêu cầu hoàn tiền và chuyển sang Kế toán?');">Duyệt</button>
	                            <button class="refund-button refund-button--danger" type="submit" name="action" value="reject" onclick="return confirm('Từ chối yêu cầu hoàn tiền này?');">Từ chối</button>
	                          </div>
                        </form>
                      <% } else { %>
                        <span class="refund-sub"><%= row.statusMeta?.hint || "Đã xử lý" %></span>
                      <% } %>
                    </td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>

          <div class="refund-pages">
            <% if (page > 1) { %><a class="refund-page" href="/manager/refunds?<%= makeQuery({ page: page - 1 }) %>">Trước</a><% } %>
            <% pages.forEach((item) => { %>
              <a class="refund-page <%= item === page ? "is-active" : "" %>" href="/manager/refunds?<%= makeQuery({ page: item }) %>"><%= item %></a>
            <% }) %>
            <% if (page < totalPages) { %><a class="refund-page" href="/manager/refunds?<%= makeQuery({ page: page + 1 }) %>">Tiếp</a><% } %>
          </div>
        <% } %>
      </section>
    </section>
  </div>
</main>

<%- include("../partials/footer") %>
