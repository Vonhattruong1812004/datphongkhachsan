<style id="customer-account-background">
  html:has(.guest-home),
  html:has(.profile-shell),
  html:has(.history-shell),
  html:has(.svc-shell),
  html:has(.adv-shell),
  html:has(.detail-shell),
  html:has(.cust-edit-shell),
  html:has(.customer-mobile-bg),
  html:has(.guest-booking-shell),
  html:has(.online-shell),
  html:has(.room-detail-shell),
  html:has(.book-shell),
  html:has(.review-shell),
  html:has(.pay-shell),
  html:has(.invoice-shell),
  html:has(.fb-shell),
  html:has(.ekyc-home),
  body:has(.guest-home),
  body:has(.profile-shell),
  body:has(.history-shell),
  body:has(.svc-shell),
  body:has(.adv-shell),
  body:has(.detail-shell),
  body:has(.cust-edit-shell),
  body:has(.customer-mobile-bg),
  body:has(.guest-booking-shell),
  body:has(.online-shell),
  body:has(.room-detail-shell),
  body:has(.book-shell),
  body:has(.review-shell),
  body:has(.pay-shell),
  body:has(.invoice-shell),
  body:has(.fb-shell),
  body:has(.ekyc-home) {
    background: #dceeff url("/images/dl1_1_1_20260411061914.webp?v=1") center / cover fixed no-repeat !important;
  }

  body:has(.guest-home),
  body:has(.profile-shell),
  body:has(.history-shell),
  body:has(.svc-shell),
  body:has(.adv-shell),
  body:has(.detail-shell),
  body:has(.cust-edit-shell),
  body:has(.customer-mobile-bg),
  body:has(.guest-booking-shell),
  body:has(.online-shell),
  body:has(.room-detail-shell),
  body:has(.book-shell),
  body:has(.review-shell),
  body:has(.pay-shell),
  body:has(.invoice-shell),
  body:has(.fb-shell),
  body:has(.ekyc-home) {
    color: #102033;
  }

  body::before {
    content: "" !important;
    position: fixed !important;
    inset: 0 !important;
    z-index: -2 !important;
    background: rgba(238, 246, 255, .08) !important;
    pointer-events: none !important;
  }

  .guest-home,
  .profile-shell,
  .history-shell,
  .svc-shell,
  .adv-shell,
  .detail-shell,
  .cust-edit-shell,
  .guest-booking-shell,
  .online-shell,
  .room-detail-shell,
  .book-shell,
  .review-shell,
  .pay-shell,
  .invoice-shell,
  .fb-shell,
  .ekyc-home {
    background: transparent !important;
  }

  .guest-home,
  .profile-shell,
  .history-shell,
  .svc-shell,
  .adv-shell,
  .detail-shell,
  .cust-edit-shell,
  .guest-booking-shell,
  .online-shell,
  .room-detail-shell,
  .book-shell,
  .review-shell,
  .pay-shell,
  .invoice-shell,
  .fb-shell,
  .ekyc-home,
  .container {
    position: relative;
    isolation: isolate;
  }

  body:has(.guest-home) [class*="-panel"],
  body:has(.guest-home) [class*="-card"],
  body:has(.profile-shell) [class*="-panel"],
  body:has(.profile-shell) [class*="-card"],
  body:has(.history-shell) [class*="-panel"],
  body:has(.history-shell) [class*="-card"],
  body:has(.svc-shell) [class*="-panel"],
  body:has(.svc-shell) [class*="-card"],
  body:has(.adv-shell) [class*="-panel"],
  body:has(.adv-shell) [class*="-card"],
  body:has(.detail-shell) [class*="-card"],
  body:has(.cust-edit-shell) [class*="-card"],
  body:has(.guest-booking-shell) [class*="-panel"],
  body:has(.guest-booking-shell) [class*="-card"],
  body:has(.guest-booking-shell) .room-fresh,
  body:has(.guest-booking-shell) .recent-room-card,
  body:has(.guest-booking-shell) .travel-room-card,
  body:has(.online-shell) [class*="-panel"],
  body:has(.online-shell) [class*="-card"],
  body:has(.room-detail-shell) [class*="-panel"],
  body:has(.room-detail-shell) [class*="-card"],
  body:has(.room-detail-shell) .room-spec,
  body:has(.room-detail-shell) .room-status,
  body:has(.room-detail-shell) .room-note,
  body:has(.room-detail-shell) .room-policy,
  body:has(.book-shell) [class*="-panel"],
  body:has(.book-shell) [class*="-card"],
  body:has(.review-shell) [class*="-panel"],
  body:has(.review-shell) [class*="-card"],
  body:has(.review-shell) .review-box,
  body:has(.pay-shell) [class*="-panel"],
  body:has(.pay-shell) [class*="-card"],
  body:has(.pay-shell) .pay-box,
  body:has(.invoice-shell) [class*="-panel"],
  body:has(.invoice-shell) [class*="-card"],
  body:has(.invoice-shell) .invoice-room,
  body:has(.fb-shell) [class*="-panel"],
  body:has(.fb-shell) [class*="-card"],
  body:has(.fb-shell) .fb-feedback-item,
  body:has(.ekyc-home) [class*="-panel"],
  body:has(.ekyc-home) [class*="-card"],
  body:has(.ekyc-home) .ekyc-stat,
  body:has(.ekyc-home) .ekyc-status-card,
  body:has(.ekyc-home) .ekyc-uploader,
  body:has(.ekyc-home) .ekyc-history-card {
    background: rgba(255, 255, 255, .78) !important;
  }

  body:has(.guest-home) .guest-section,
  body:has(.history-shell) .history-section,
  body:has(.svc-shell) .svc-section,
  body:has(.adv-shell) .adv-section,
  body:has(.guest-booking-shell) .booking-section,
  body:has(.online-shell) .booking-section,
  body:has(.room-detail-shell) .room-section,
  body:has(.book-shell) .book-section,
  body:has(.review-shell) .review-section,
  body:has(.pay-shell) .pay-section,
  body:has(.invoice-shell) .invoice-section,
  body:has(.fb-shell) .fb-section,
  body:has(.ekyc-home) .ekyc-section {
    border: 1px solid rgba(216, 225, 236, .76) !important;
    border-radius: 12px !important;
    padding: 16px !important;
    background: rgba(255, 255, 255, .72) !important;
    box-shadow: 0 18px 45px rgba(15, 23, 42, .08) !important;
  }

  body:has(.guest-home) .guest-section-head,
  body:has(.history-shell) .history-section-head,
  body:has(.svc-shell) .svc-section-head,
  body:has(.adv-shell) .adv-section-head,
  body:has(.guest-booking-shell) .booking-section-head,
  body:has(.online-shell) .booking-section-head,
  body:has(.room-detail-shell) .room-section-head,
  body:has(.book-shell) .book-section-head,
  body:has(.review-shell) .review-section-head,
  body:has(.pay-shell) .pay-section-head,
  body:has(.invoice-shell) .invoice-section-head,
  body:has(.fb-shell) .fb-section-head,
  body:has(.ekyc-home) .ekyc-section-head {
    border-radius: 10px !important;
    padding: 8px 10px !important;
    background: rgba(255, 255, 255, .82) !important;
  }

  body:has(.guest-home) .guest-section-head h2,
  body:has(.guest-home) .guest-section h2,
  body:has(.profile-shell) .profile-panel h2,
  body:has(.profile-shell) .profile-ekyc-head h2,
  body:has(.history-shell) .history-section-head h2,
  body:has(.svc-shell) .svc-section-head h2,
  body:has(.adv-shell) .adv-section-head h2,
  body:has(.guest-booking-shell) h1,
  body:has(.guest-booking-shell) h2,
  body:has(.online-shell) h1,
  body:has(.online-shell) h2,
  body:has(.room-detail-shell) h1,
  body:has(.room-detail-shell) h2,
  body:has(.book-shell) h1,
  body:has(.book-shell) h2,
  body:has(.review-shell) h1,
  body:has(.review-shell) h2,
  body:has(.pay-shell) h1,
  body:has(.pay-shell) h2,
  body:has(.invoice-shell) h1,
  body:has(.invoice-shell) h2,
  body:has(.fb-shell) h1,
  body:has(.fb-shell) h2,
  body:has(.ekyc-home) h1,
  body:has(.ekyc-home) h2 {
    color: #0f2442 !important;
    text-shadow: none !important;
  }

  .guest-home::before,
  .guest-home::after,
  .profile-shell::before,
  .profile-shell::after,
  .history-shell::before,
  .history-shell::after,
  .svc-shell::before,
  .svc-shell::after,
  .adv-shell::before,
  .adv-shell::after,
  .detail-shell::before,
  .detail-shell::after,
  .cust-edit-shell::before,
  .cust-edit-shell::after,
  .guest-booking-shell::before,
  .guest-booking-shell::after,
  .online-shell::before,
  .online-shell::after,
  .room-detail-shell::before,
  .room-detail-shell::after,
  .book-shell::before,
  .book-shell::after,
  .review-shell::before,
  .review-shell::after,
  .pay-shell::before,
  .pay-shell::after,
  .invoice-shell::before,
  .invoice-shell::after,
  .fb-shell::before,
  .fb-shell::after,
  .ekyc-home::before,
  .ekyc-home::after {
    animation: none !important;
  }
</style>
